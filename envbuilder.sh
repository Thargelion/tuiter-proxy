{
    echo "${ENV_APP}"
    echo "${ENV_LOG}"
    echo "${ENV_DB}"
    echo "${ENV_SESSION}"
    echo "${ENV_CACHE}"
    echo "${ENV_REDIS}"
    echo "${ENV_MAIL}"
    echo "${ENV_AWS}"
    echo "${ENV_HOST}"
} > .env
